export const SCALE = 100;

export const EXTRUSION_DEPTH = 0.5 * SCALE;

export const WIDTH = 32 * SCALE;
export const HEIGHT = EXTRUSION_DEPTH;
export const DEPTH = 42 * SCALE;

export const WALL_HEIGHT = 3.2 * SCALE;
export const WALL_WIDTH = 1 * SCALE;

export const HOLE_RADIUS = 3.7 / 2 * SCALE;

export const BALL_RADIUS = 0.5 * SCALE;

export const STICK_RADIUS = 0.5 * SCALE;
export const STICK_HEIGHT = 2.5 * SCALE;

export const MAGNET_RADIUS = 0.2 * SCALE;
export const MAGNET_HEIGHT = 0.5 * SCALE;

export const CAMERA_Z = 40 * SCALE; // 40
export const CAMERA_Y = 35 * SCALE; // 35